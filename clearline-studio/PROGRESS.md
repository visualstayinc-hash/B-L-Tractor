# PROGRESS.md — Clearline Studio

Persistent state. If context resets or a new session begins, read this file first (after `pwd`), then QUALITY_GATE.md, CONCEPT.md, tests/*.md, then check `git status` and recent commits, then run the app, then continue from the first unresolved FAIL in QUALITY_GATE.md. Never restart from memory.

## Location

`/home/user/B-L-Tractor/clearline-studio/` — single-file site at `clearline-studio/index.html`, control docs at the paths listed below.

## File map

- `CONCEPT.md` — locked creative direction. Do not drift from it silently.
- `QUALITY_GATE.md` — authority on pass/fail. Read before claiming anything is done.
- `benchmarks/` — reference notes (mostly BLOCKED — no live network access in this sandbox).
- `tests/visual-checklist.md`, `tests/functional-checklist.md` — test list, written before implementation.
- `scripts/verify.sh` — Playwright-based verification runner.
- `artifacts/visual/` — screenshots + index.md, evidence for QUALITY_GATE.md rows.
- `artifacts/final/` — final evidence package (created at the end, not before).
- `FINAL_REVIEW.md` — written last.

## State log

- [DONE] CONCEPT.md written and locked.
- [DONE] benchmarks/ written (honest — most entries BLOCKED, no live network access).
- [DONE] QUALITY_GATE.md skeleton written, all rows FAIL/BLOCKED.
- [DONE] PROGRESS.md, tests/*.md, scripts/verify.sh.
- [DONE] Pass 1 build (index.html — concept + composition).
- [DONE] Pass 1 verification. Real bugs found and fixed: mobile overflow (unbroken email text), a test-tooling false negative in verify.js itself (fixed by scrolling for real before screenshots), and a layout-thrashing read-after-write bug in render(). QUALITY_GATE.md updated with real evidence — see it for the full row-by-row state. **Known FAILs carried into Pass 2**: F03 (buttons untested), F06 (keyboard nav untested), F07 (hover untested), F08 (scroll settle untested), F10 (form validation untested), S05 (`.crosshair` CSS class defined but never applied — dead code), MO01/MO02 (no dedicated motion-hierarchy review yet), C04 (no persistent contact affordance — real UX gap, not just untested).
- [DONE] Pass 2 (interaction + motion). Fixed: C04 (persistent "Start a line" link in the spine), S05 (real coordinate readout replacing dead `.crosshair` CSS), PF04 (layout-thrashing read-after-write in `render()`). Tested and confirmed PASS with real evidence: F03, F06, F07, F10, MO01, MO02. Two false negatives found and fixed in the *test scripts themselves* (not the site) — see artifacts/visual/index.md. **Remaining known FAILs, deliberately deferred, not forgotten**: F08 (scroll-settle jank), F09/M02/M04 (touch-specific spine drag — only mouse-pointer-tested so far), F14 (reduced motion), PF03 (layout shift across load states).
- [DONE] Pass 3 (typography + image + portfolio). Real bugs found and fixed: (1) all six portfolio plots shared an identical layout — fixed with alternating `.plot--rev` sides; (2) that fix itself caused a mobile overflow regression via a CSS-specificity conflict with the responsive media query — caught immediately and fixed; (3) the tick-clustering noted visually in Pass 1 turned out to be a real functional bug, not cosmetic — `offsetTop` was relative to the wrong ancestor for nested `.plot-N` sections, corrupting the drag-snap target math for 6 of 9 ticks. Fixed with a `getBoundingClientRect()`-based `docTop()` helper used everywhere instead. Typography and image-strategy honesty both held up under dedicated review, no changes needed there.
- [DONE] Pass 4 (responsive + micro-polish + performance). All real, with real touch events (CDP `Input.dispatchTouchEvent`) confirming F09/M02/M04. Reduced motion confirmed genuinely instant (F14) — spine spring skipped entirely, draw-on skipped entirely, both verified by frame-by-frame sampling, not just code inspection. F08's one flagged "stuck" element turned out to be mid-transition at the exact moment sampled (0.29px offset, resolved to exactly 0px given more time) — investigated and confirmed not a real bug. PF03 (layout shift) confirmed zero reflow. **QUALITY_GATE.md: every row is now PASS.**
- [DONE] Final evidence package + FINAL_REVIEW.md + ship (commit/push/artifact/deliver).
- [DONE] **Scoped follow-up: real contact info integration.** Replaced the placeholder `studio@clearline.example` with the real `clear.line.studio.creative@gmail.com` everywhere it appeared; added real phone (`806-731-9998` / `tel:+18067319998`) and address to the contact section's direct-contact block, tiered by visual weight (email primary, phone secondary, address tertiary/non-linked), inheriting the existing `.contact-direct a` link styling rather than introducing new components. Made the "Send" mailto link build its subject/body from the form's actual typed contents instead of opening blank. This was explicitly scoped as an implementation task, not a redesign — no other section was touched. One real overflow regression (from `align-self:flex-start` removing the width constraint needed for text-wrapping) was caught by `verify.sh` and fixed with `max-width:100%`. A focus-indicator check initially gave a false negative from using programmatic `.focus()` instead of a real keyboard Tab press — redone correctly, confirmed passing. Full regression check re-ran the spine drag, draw canvas, coordinate readout, and spine contact link — all still work. See `artifacts/visual/index.md` for the full account with evidence.
- [DONE] **Follow-up: real email delivery, replacing mailto.** The form's "Send" no longer opens the visitor's mail client — it POSTs to Web3Forms (`https://api.web3forms.com/submit`), a no-backend form-delivery service, with real loading/success/error UI and a client-side honeypot. `#cf-submit` is now a real `<button type="submit">`, not an `<a href="mailto:...">`. `grep -n "mailto:" index.html` confirms exactly one `mailto:` remains anywhere in the file — the standalone email link, explicitly permitted. **Delivery to `clear.line.studio.creative@gmail.com` is BLOCKED, not verified**: it needs a real Web3Forms access key, which only the inbox owner can obtain (requires their own email verification with Web3Forms) — this cannot be fabricated without either faking a working state or attempting an account action on someone else's inbox, neither of which happened. Tested everything that could be tested from here: the "not configured" guard fires correctly with zero network calls; the honeypot and native-validation paths correctly block with zero network calls; a temporary (reverted, never committed) test key confirmed a *real* outbound request genuinely fires and is correctly handled through to the error UI when it fails — it failed with `net::ERR_TUNNEL_CONNECTION_FAILED`, this sandbox's own network policy blocking the destination domain, not a code defect. One new overflow bug (same root cause as bug #1 — an unbroken email address in a narrow column) was found in the error-state text and fixed the same way. See `artifacts/visual/index.md` and `FINAL_REVIEW.md` for full evidence and the exact one-time setup step required.
- [DONE] **Follow-up: final contact experience refinement.** A dedicated visual review of the live site (not source-reading) found two real gaps: (1) the static-email disclosure ("opens your mail client, nothing submitted server-side") had been silently dropped during the prior restructure — fixed with a small caption matching the draw-canvas caption's voice; (2) the full address (city/state/zip added in this follow-up: "3312 Ault Dr, Amarillo, TX 79121") had no location interaction — fixed by making it a Google Maps search link with a hover-revealed underline (kept subtle/tertiary at rest, per the brief's "don't add a giant generic map" instruction). The longer address was re-tested against the same narrow mobile column that has overflowed twice before in this project — confirmed wrapping correctly with zero overflow. Full regression re-check (spine drag, draw canvas, dynamic mailto) confirmed nothing else was disturbed.

## Known environment constraints (do not re-discover these — they are already established)

- No live outbound network access to arbitrary domains. Google Fonts CSS/font fetches fail with `ERR_CONNECTION_RESET` in Playwright — this is expected and not a bug to fix; the fallback stack must render acceptably (F13).
- Playwright is available via the global npm install at `/opt/node22/lib/node_modules` — invoke with `NODE_PATH=/opt/node22/lib/node_modules node <script>.js`, not `npx playwright`.
- Playwright's `.click({ })` actionability pre-check can misreport "intercepts pointer events" for `position:fixed` elements even when `elementFromPoint` confirms the target is correctly on top — cross-check with `elementFromPoint` before treating that specific error as a real bug (established during the Texas Pride Concrete build earlier this session).
- A real, reproducible Chromium bug: an element that is both an `IntersectionObserver` target *and* carries `clip-path` stops receiving further intersection callbacks once clipped to zero visible area. Never observe a clipped element directly — observe an unclipped ancestor and reveal descendants from its callback.
- **This sandbox's outbound network policy blocks `api.web3forms.com` (and any domain outside a small allowlist) at the proxy/gateway level, confirmed directly via `curl -sS "$HTTPS_PROXY/__agentproxy/status"`**, which logs `"connect_rejected"` / `"gateway answered 403 to CONNECT"` for that host. This means the real Web3Forms contact-form integration cannot be verified end-to-end (actual inbox delivery) from inside this development environment — not because of a code or key problem, but because the sandbox itself refuses the connection before it reaches Web3Forms' servers. The code is correct and configured with a real key; only a human testing the *deployed* artifact (which runs in their own browser, outside this sandbox's proxy) can verify actual delivery. Do not re-attempt to "fix" this from inside the sandbox — there is nothing further to fix here.

- [DONE] **Follow-up: brand + positioning correction — introduce Evan Greene, sharpen the thesis.** Inspection of the running site confirmed the brief's critique was accurate: no visible wordmark (only an `aria-label`), no person named anywhere, and an abstract hero thesis that never named a deliverable. Fixed: hero lockup ("Clearline Studio" wordmark + "Evan Greene — independent design & build" byline, neither one dominating), a sharpened thesis naming the actual deliverable (websites and digital products), the Process section's intro reframed in Evan's voice, and a closing hand-drawn signature credit at the end of Process — a genuinely new, fourth application of the line-as-device concept (signature), reusing the existing draw-on mechanism with zero new JS. No fabricated credentials, years, clients, or awards were added — only the name, role, and craft statement the user supplied. Full regression re-verified (spine drag, draw canvas); no overflow on mobile (byline/signature both confirmed wrapping cleanly). See `artifacts/visual/index.md` for full evidence, including one motion-timing false alarm investigated and resolved the same way prior ones in this project were (mid-transition, not stuck).
- [DONE] **Follow-up: configure the real Web3Forms access key.** User supplied `c93a9106-98f4-4d02-b5fb-5e541e8ad9bf` (obtained for `clear.line.studio.creative@gmail.com`). Configured in `index.html`; the guard's comparison string was deliberately left as the original placeholder so it now genuinely differs from the real key and the real fetch path executes. A full real-key end-to-end test (desktop + mobile) was run and failed with `net::ERR_TUNNEL_CONNECTION_FAILED` — root cause conclusively confirmed as this sandbox's own egress policy (see note above), not the code or the key. Loading/error states, form-data preservation on failure (no `form.reset()` outside the success branch), and mobile overflow were all re-verified clean. **Actual inbox delivery remains BLOCKED, and can only be verified by a human testing the live deployed artifact directly.**
- [DONE] **Follow-up: differentiation system.** Before writing anything, three requested "differentiators" (Evan's age/ambition framing, in-person appointment availability, a proactive pre-contact preview practice) were factual claims about a real person/business, not design choices — asked the user directly via AskUserQuestion rather than assuming or inventing them, consistent with this project's standing anti-fabrication discipline. Confirmed: age is implied, not stated directly; in-person appointments are real (stated as fact); the pre-contact preview practice is real and established (described with conditional language, not guaranteed). Added a new `#difference` section between Work and Process — not a generic "why us" grid, but a five-step horizontal dimension-line diagram (Discovered → Research → Concept → Preview → Meet) reusing the exact visual pattern already established by the Process section's own diagram (thin line + dots + numbered stages), recolored for a light section instead of copied verbatim. The "Preview" stage is a real click/tap-toggle disclosure (`aria-expanded`, verified via mouse click AND a real keyboard Enter press, not hover-only — hover-only interactions don't work on touch, a lesson already learned earlier in this project), revealing additional detail on genuine user action rather than being decorative. New copy states the local/Texas-Panhandle service area and in-person availability as fact, and the preview practice with explicit conditional language ("may," "sometimes," "occasionally," "depending on the business") plus an explicit non-guarantee disclosure line. A claim-validation grep pass confirmed zero banned absolute claims ("always," "every client," "guaranteed"), zero banned marketing clichés, and that all "client" occurrences in the file are pre-existing, already-audited, non-fabrication uses (DOM API names, mail-client terminology, the honesty disclosure about concept studies) — no new ones introduced. The new section registers automatically in the spine nav (`data-sec="difference"` — the spine builds ticks dynamically from `[data-sec]`, confirmed via the tick-label list including "DIFFERENCE" in the correct position, no manual spine code needed). Full regression re-verified: spine drag, contact-method hrefs, form fields, keyboard tab order (unaffected — the new section sits after `#work`, before the contact form in document order), and `prefers-reduced-motion` (the new diagram's line resolves instantly with no scroll needed, matching the site's one motion rule). Zero overflow at 390/1440px in both the collapsed and expanded reveal states. See `artifacts/visual/index.md` for full evidence and `QUALITY_GATE.md` V20-V23 (renumbered from the brief's requested V19-V22 since V19 was already in use by the contact-experience-replacement gate from the prior follow-up — noted explicitly rather than silently overwriting or duplicating an ID).
- [DONE] **Follow-up: contact experience replacement — removed the draw-canvas, rebuilt as a direct conversion-oriented layout.** The "draw a line" gesture canvas (`#drawCanvas`, `#clearCanvas`, and all supporting CSS/JS) was removed entirely — `grep -n -i "drawcanvas\|clearcanvas\|canvaspoint"` confirms zero remaining references. Replaced with a "Start the project." panel: a one-sentence identity/positioning statement (Evan Greene, Clearline Studio, web design & development, Texas Panhandle — reusing exact language from the prior positioning pass), then tiered `.contact-methods` (email/call/visit, replacing the old `.contact-direct` block that sat below the form) with the same email>phone>address visual hierarchy the project has maintained since the original contact-info integration. The Web3Forms form itself (honeypot, native validation, loading/success/error states, `WEB3FORMS_ACCESS_KEY`/`WEB3FORMS_ENDPOINT`/submit handler) was untouched — re-verified working identically to before (invalid submission blocked client-side with zero network calls; valid submission shows "Sending…" + disabled button, then the expected sandbox-network error state, exactly matching prior documented behavior). New micro-interaction: a thin line (`#contactProgressPath`) that draws in as the visitor fills each of the three required fields (0/3 → fully hidden, 3/3 → fully drawn), replacing the old free-draw gesture with something that's actually informative — reuses the site's one motion rule (`stroke-dashoffset`) rather than introducing a new animation type. Two real bugs found and fixed during verification, not just claimed working: (1) a stray dot rendered at 0% progress from an SVG `stroke-linecap:round` artifact at the dash boundary — fixed by switching to `butt` caps; (2) that fix alone wasn't enough — a single-value `stroke-dasharray` creates a repeating dash/gap pattern, and the offset used briefly wrapped into the *next* dash cycle, painting a sliver near the path's tail even with butt caps — fixed properly by giving the dasharray an explicit oversized gap (`L 999`) so only one dash ever exists in the pattern. (3) A genuine, unrelated pre-existing bug also surfaced during mobile screenshot review: `.contact-grid` had no mobile breakpoint at all — it was rendering its two-column desktop grid squeezed into the 390px viewport instead of stacking, for both the old canvas layout and the new one. Fixed with a `@media (max-width:880px)` rule that switches to a single column and reorders the form to lead (primary conversion action first on mobile), matching the brief's explicit "do not merely stack the desktop version" requirement. The spine's persistent CTA ("Start a line") was also updated to "Start a project" since the interaction it named no longer exists. Full regression re-verified: spine drag, all three contact method hrefs, form field presence, zero overflow at 390/768/1024/1440px, zero new console errors (only the pre-existing Google Fonts block). See `artifacts/visual/index.md` entries 34-37.
- [DONE] **Follow-up: positioning clarity patch (content only, no visual redesign).** The hero named the studio and the person but never stated the actual function or service area explicitly — a real clarity gap, distinct from the earlier brand-identity work. Two copy-only changes, no markup/CSS/JS structure touched: (1) the byline changed from "Evan Greene — independent design & build" (vague per the brief's own list of banned vague phrases) to "Evan Greene — web design & development" (states the function); (2) the hero thesis was rewritten from an abstract line about "websites and digital products... the way this line was drawn" to an explicit positioning statement: "Clearline Studio is Evan Greene's web design studio, building websites and digital products for businesses across the Texas Panhandle. Each one is designed and built with the same deliberate precision as this line." — covering brand (Clearline Studio), owner (Evan Greene), function (web design studio), and service area (Texas Panhandle) in one sentence, with the concept's line-metaphor preserved in a second sentence rather than dropped. Meta description updated to match for consistency. Verified: all four facts appear within the first viewport (`.home-hint` bounding box confirmed fully within a 900px-tall viewport, no scroll needed) at both 1440px and 390px, zero overflow, and `git diff --stat` confirms exactly one file changed with only the meta/byline/thesis lines touched — no accidental scope creep into brand system, layout, or interactions. Did not add a supporting founder section, badge, pill, or map — the hero rework alone satisfies the five-second test the brief specified. Full regression (`verify.sh` + spine drag/draw canvas/contact form) re-run clean. See `artifacts/visual/index.md` entry 31.
- [DONE] **Follow-up: mark geometry + color system correction, once the real brand board image was actually provided (not just described).** Two genuine, verifiable mismatches found by direct inspection of the board's six panels: (1) the mark is a single straight diagonal stroke everywhere on the board, not the site's nine-point zigzag — replaced `#markPath`'s `d` with `M260 195 L460 25`, kept the existing `stroke-dashoffset` draw-on mechanism unchanged (verified via Playwright: `strokeDashoffset` still resolves to `0` after the intersection fires); (2) the board is strictly black/white across all six panels, including the invoice mockup, with zero accent color anywhere — the site's invented drafting-red accent (`--red`) was systemically load-bearing (15+ usages: eyebrows, plot numbers, coordinate readouts, process diagram highlights, the signature mark, spine active states, the visitor's drawn stroke, focus/error states) but unsupported by the actual reference. Converted every usage to `--ink` (light sections) or `--paper` (dark sections), matching the existing light/dark inversion pattern already used elsewhere on the page; the contact form's error state now signals via weight (ink + bold) instead of hue. Deleted the now-unused `--red`/`--red-2` variables. Full regression (spine drag, draw canvas, contact form fields) and full-suite `verify.sh` (zero overflow, zero unexpected console errors) both re-run clean at 1440px and 390px after the change. See `artifacts/visual/index.md` entries 25-30 and `CONCEPT.md`'s "BRAND SYSTEM CORRECTION" section for the full reasoning, including what was deliberately not adopted (the board's horizontal lockup variant) and why.
- [DONE] **Follow-up: wordmark lockup refinement from the real brand identity board.** The user supplied an actual Clearline Studio brand board (photographed moodboard, not re-viewable across a later context reset — its specific facts are recorded in CONCEPT.md's "WORDMARK LOCKUP" section so they survive). Genuine gap found: the board's wordmark is a two-tier lockup (wide-tracked "CLEARLINE" over smaller-tracked "STUDIO"), the live hero instead ran it as one mixed-case line. Fixed by restructuring `.hero-wordmark` into `.hero-wordmark__main`/`.hero-wordmark__sub`, wrapped in one `aria-label`led `<h1>` with `aria-hidden` spans so screen readers still hear one clean name. First CSS attempt (`align-items:baseline`) misaligned the byline against the two-line stack — caught by screenshot, not assumed correct — fixed with `align-items:flex-end`. Deliberately declined to adopt the board's placeholder invoice contact info or its "REAL RESULTS" tagline copy (unsubstantiated outcomes claim) or swap in its single-diagonal mark (bigger, riskier change deferred to a future pass with the actual board in hand). Full regression re-verified: spine drag, draw canvas, contact form fields, zero new console errors, zero overflow at 1440px/390px. See `artifacts/visual/index.md` entries 25-27.
