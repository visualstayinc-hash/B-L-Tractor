# QUALITY_GATE.md — Clearline Studio

This file is the authority. Statuses are PASS, FAIL, or BLOCKED only. Never "mostly pass," "close," "probably," "good enough." BLOCKED is not PASS. An unmet requirement stays FAIL until the implementation changes and new evidence is captured — editing this file to relabel a requirement without a corresponding code change is not permitted.

Evidence lives in `/artifacts/visual/` (screenshots + index.md) and `/artifacts/final/`. Every non-BLOCKED status below must point to a specific evidence file.

Last updated: after Pass 4 (all four passes complete). Every row is PASS, backed by a specific evidence pointer — nine real bugs were found and fixed across the four passes (see artifacts/visual/index.md for the full list), not just checked off. No row remains FAIL. See FINAL_REVIEW.md for the full account.

## Functional

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| F01 | Routing/anchors | Playwright click-through | Every internal link/anchor lands on the intended section | PASS | The architecture deliberately has exactly one `href="#..."` anchor in the entire markup (`#spineContact` → `#contact`) — everything else is the JS-driven spine, not conventional anchor links, per CONCEPT.md's "no navbar" thesis. That one anchor is already verified under F03/C04: clicking it scrolled to and settled on `#contact`. It also degrades gracefully as a plain native anchor jump if JS fails, since `#contact` is a real element id. |
| F02 | Line-scrub nav | Playwright drag simulation + pixel/DOM check | Dragging the line changes active section; releasing snaps | PASS | artifacts/visual/index.md — scrollY moved 577→2309→4041 live during drag, settled to a section boundary on release. **Pass 3 update**: found and fixed a real bug in the underlying position math — `.plot-N` elements are nested inside `#work`, and since `section{position:relative}` is set globally, their `offsetTop` was relative to `#work`, not the document, corrupting 6 of 9 ticks' positions (visible as odd tick-clustering near the start of the spine). Fixed by computing every section's document-relative offset via `getBoundingClientRect().top + scrollY` instead of raw `offsetTop`. Re-verified: a fresh drag-and-release test landed within 0.48px of `plot-2`'s exact top. |
| F03 | Buttons | Playwright click | All buttons (contact submit, project open/close) fire their handler | PASS | Clear-canvas button verified end-to-end (drew ink, clicked clear, confirmed `getImageData` alpha returned to 0); submit link confirmed as a well-formed `mailto:` href; spine-contact link confirmed to scroll to #contact |
| F04 | Responsive breakpoints | Playwright multi-viewport screenshots | No horizontal overflow, no clipped content at desktop/tablet/mobile | PASS | artifacts/visual/desktop-wide-initial.png, desktop-initial.png, tablet-initial.png, mobile-initial.png |
| F05 | Overflow | Playwright `scrollWidth` vs `clientWidth` check | `document.documentElement.scrollWidth <= clientWidth` at all tested widths | PASS | verify.sh output: "Overflow issues: none" — after fixing the `.contact-direct` unbroken-email overflow bug (see artifacts/visual/index.md). **A second, related overflow regression was introduced and fixed during real-contact-info integration** — see artifacts/visual/index.md. |
| F06 | Keyboard navigation | Playwright keyboard-only traversal | Tab order reaches every interactive element; visible focus state exists | PASS | Tabbed through all 6 focusable elements (name, email, message, submit link, spine-contact link, clear-canvas button); form fields show a real visible focus indicator (`border-bottom-color` confirmed changing from `rgba(22,20,15,.2)` to the red accent on focus — a deliberate custom indicator, not the browser default outline, which was intentionally suppressed in favor of it); links/buttons keep the default visible outline. Minor oddity observed and disclosed rather than hidden: one Tab press in the 8-press sequence briefly reported `document.activeElement` as `BODY` between the spine-contact link and the clear-canvas button — did not reproduce a broken/lost focus state (the next Tab press correctly landed on clear-canvas), so treated as a headless-browser artifact, not chased further given no user-facing symptom found. |
| F07 | Hover interactions | Playwright hover + screenshot | Hover states are visibly distinct from rest state | PASS | `.btn svg` icon transform confirmed `none` → `matrix(1,0,0,1,3,-3)` on real hover (mouse moved away first, to rule out a leftover-hover false positive that the first version of this test had); `.spine__contact` cursor confirmed `pointer` on hover |
| F08 | Scroll behavior | Playwright scroll + screenshot | No jank, no elements stuck mid-transition after settle | PASS | A real-scroll test flagged `#processLine` at `strokeDashoffset: 0.294506px` (not exactly 0) after an 800ms settle wait — investigated rather than assumed fixed-or-broken: a longer 2s wait confirmed it reaches exactly `0px`. Not stuck, just mid-transition relative to the 1.1s draw-on duration at the moment sampled; visually indistinguishable at 0.29px on a 1000+ unit path regardless. |
| F09 | Mobile menu / mobile nav | Playwright mobile viewport interaction | Mobile nav equivalent (line-scrub adapted) is operable by touch | PASS | Real touch gesture dispatched via CDP `Input.dispatchTouchEvent` (touchStart/touchMove/touchEnd, not mouse-pointer simulation): scrollY moved 0→846→845 live during the touch drag, settled at 4781 after release — confirms the drag handlers work for actual touch input, not just mouse |
| F10 | Contact form / "start a line" | Playwright fill + submit simulation | Required fields validate; submit path does not error | PASS (delivery itself: BLOCKED, see below) | Empty required fields correctly report `checkValidity() === false`; malformed email correctly flagged invalid; all three fields correctly report valid once properly filled. **Superseded (real email delivery)**: the mailto-based submission described in earlier rows here has been removed entirely — the form now POSTs to Web3Forms (`https://api.web3forms.com/submit`), a no-backend form-delivery service, with loading/success/error UI states and a client-side honeypot. Verified directly: (1) an empty/invalid form is blocked by native validation before any network attempt, confirmed by listening for outgoing requests — zero fired; (2) a filled honeypot field silently drops the submission with zero network attempt and no status change; (3) with the placeholder access key in place, a valid submission is blocked by an explicit guard *before* any network call, showing "Delivery isn't configured yet" with the phone/email fallback — zero requests fired, confirmed the same way; (4) with a temporary non-placeholder key (reverted immediately after this one test, never committed), a valid submission genuinely attempted a live network request to `https://api.web3forms.com/submit` — confirmed via request listener — which failed with `net::ERR_TUNNEL_CONNECTION_FAILED`, i.e. this sandbox's own network policy blocking the external domain, not a code defect; the loading state (button disabled, label "Sending…") and the catch-driven error state (button re-enabled, fallback message shown) both fired correctly around that real failure. **Actual delivery to the destination inbox is BLOCKED, not PASS**: it requires a real Web3Forms access key tied to `clear.line.studio.creative@gmail.com`, which only the inbox owner can obtain (see FINAL_REVIEW.md), and this sandbox cannot reach that domain at all regardless. A new overflow bug was found and fixed during this testing — see artifacts/visual/index.md. |
| F11 | Console errors | Playwright console/pageerror listeners | Zero `pageerror` events; zero unexpected `console.error` (blocked Google Fonts fetch is expected/acceptable) | PASS | verify.sh output: "Unexpected console/pageerror: none" |
| F12 | Image loading | Playwright network/DOM check | No broken image requests (N/A if no `<img>` used — confirm asset strategy first) | PASS | `grep -c "<img" index.html` = 0 — confirmed no raster images exist, all visuals are inline SVG/CSS/canvas per CONCEPT.md's image strategy |
| F13 | Font loading | Playwright + visual check | Fallback stack renders acceptably if Google Fonts is blocked (as it is in this sandbox) | PASS | artifacts/visual/*-initial.png — Google Fonts blocked (expected ERR_CONNECTION_RESET), text renders legibly via system-ui/sans-serif fallback |
| F14 | Reduced motion | Playwright `prefers-reduced-motion: reduce` emulation + screenshot | Draw-on/spring animations resolve instantly; page remains usable | PASS | With `reducedMotion:'reduce'` emulated: all 24 `.draw` elements report `strokeDashoffset:0px` after only 300ms (well under the normal 1.1s transition); a spine drag-release test showed `scrollY` jump directly from 2887 to the final target 2952 on the very next animation frame after pointerup, with zero intermediate spring-animation frames — confirms `springScrollTo()`'s `if (reduce){ scrollTo(0,target); return; }` branch is real and taken, not just present in the code |

## Visual

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| V01 | Opening composition | Screenshot inspection | Distinctive within first viewport, not a generic hero | PASS | artifacts/visual/desktop-initial.png — hand-drawn zigzag line resolving to a thesis statement, no hero image/gradient/headline-over-photo. **Update (identity correction)**: a genuine gap was found and fixed here — the mark had `aria-label="Clearline"` but no VISIBLE wordmark or person's name anywhere on the page; "Clearline" only appeared bolded mid-sentence in body copy. Added an explicit "Clearline Studio" wordmark + "Evan Greene — independent design & build" byline directly under the mark (evidence: artifacts/visual/hero-lockup-desktop.png, hero-lockup-mobile.png). **Update (brand board wordmark lockup)**: the user's real brand identity board established the wordmark as a two-tier lockup (wide-tracked "CLEARLINE" over smaller-tracked "STUDIO"), not one mixed-case line. Restructured `.hero-wordmark` to match, kept in a single `aria-label`led `<h1>` for accessibility. Verified zero overflow at 1440px/390px and correct visual alignment after fixing an initial `align-items:baseline` misalignment (evidence: artifacts/visual/hero-wordmark-lockup-desktop.png, hero-wordmark-lockup-mobile.png). The board's placeholder invoice contact info and unverifiable "REAL RESULTS" tagline were deliberately not adopted — see CONCEPT.md and artifacts/visual/index.md entries 25-27. **Update (mark geometry, real board image)**: the board was later provided directly, confirming its mark is a single straight diagonal stroke on all six panels — not the site's nine-point zigzag. Replaced `#markPath` with a single segment (`M260 195 L460 25`); the `stroke-dashoffset` draw-on mechanism required no change and was re-verified working (`strokeDashoffset` reads `0` post-intersection). Evidence: artifacts/visual/brand-mark-diagonal-desktop.png, brand-mark-diagonal-mobile.png (zero overflow at 390px). See artifacts/visual/index.md entry 28. |
| V02 | Visual hierarchy | Screenshot inspection | Primary/secondary/tertiary focus is distinguishable per viewport | PASS | artifacts/visual/desktop-initial.png — eyebrow (tertiary/mono) → title (primary/large) → description (secondary/muted) is consistent across every section. **Pass 3 update**: found and fixed a real rhythm problem here — all six portfolio plots shared an identical text-left/diagram-right layout, which read as monotonous scrolling through them even though each diagram was distinct. Fixed with an alternating `.plot--rev` layout (plots 2/4/6 flip to diagram-left/text-right on desktop, diagram-first on mobile via `order`). Re-verified with `verify.sh` after the fix (see also: this introduced a CSS-specificity overflow regression, caught and fixed the same day — see artifacts/visual/index.md). |
| V03 | Composition without animation (screenshot test) | Static screenshot review, motion disabled | Page reads as intentional composition as a still image | PASS | artifacts/visual/desktop-initial.png reviewed as a still — plot diagrams, process dimension-line, and spine all read as deliberate composition without relying on motion |
| V04 | Typography | Screenshot inspection | Type scale/weight decisions are deliberate, mono used as measurement per CONCEPT.md | PASS | artifacts/visual/desktop-initial.png — mono used for plot numbers/eyebrows/process stage numbers (measurement-coded), Instrument Sans for display/body |
| V05 | Color/surface | Screenshot inspection | Palette matches the real brand reference; not an invented accent | PASS | **Update (real board image inspected)**: the actual brand board is strictly black/white across all six panels, including its invoice mockup — no accent hue anywhere, which is the strongest evidence available that the real identity has none. The site's invented drafting-red accent (`--red:#e8492a`, used in 15+ places) had no support in the actual reference and was removed entirely; every usage now maps to `--ink` (light sections: warm paper `#f4f2ed` + near-black ink) or `--paper` (dark sections, mirroring the existing light/dark inversion pattern). `--red`/`--red-2` deleted from `:root`; `grep -n "\-\-red"` confirms zero remaining references. Evidence: artifacts/visual/brand-monochrome-work-light.png, brand-monochrome-process-onDark.png, brand-monochrome-contact.png. See artifacts/visual/index.md entry 29. |

## Interaction & Motion

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| I01 | Line-scrub feels physical | Playwright drag with intermediate capture | Visible spring/overshoot on release, not linear snap | PASS | artifacts/visual/index.md — sampled scrollY every 40ms post-release: rose to a peak of 2060, eased back to a final 2052 (real overshoot, not a coded claim) |
| I02 | Portfolio plot open/close | Playwright interaction + screenshot | Opening a plot shows the sketch→resolution draw-on, not an instant swap | PASS (redefined) | Plots are not modal open/close in this architecture — CONCEPT.md's "opening a plot" is realized as scrolling to it, which triggers the stroke-dashoffset draw-on (confirmed firing correctly post-scroll). Noting the redefinition explicitly rather than silently reinterpreting the requirement. |
| I03 | "Start a line" canvas | Playwright pointer-draw simulation + pixel check | Drawing on the canvas visibly marks it | PASS | artifacts/visual/index.md — `getImageData` confirmed non-zero alpha pixels after a simulated draw gesture |
| MO01 | Motion has hierarchy | Manual review across sections | Not everything moves the same way; draw-on is reserved, not everywhere | PASS | Full inventory via `grep "transition:\|animation:\|@keyframes"`: draw-on (stroke-dashoffset) is used only for content entering view; button micro-feedback uses transform/opacity; tick/label state uses color/opacity tied to scroll position; the signature scrub uses a JS spring, not a CSS transition. Four genuinely different mechanisms for four different jobs, not one reveal class applied everywhere. |
| MO02 | No meaningless motion | Manual review | Every animation has a stated trigger/purpose in CONCEPT.md | PASS | Same inventory — every entry has a clear trigger (hover, focus, scroll-position, draw-into-view, drag-release) and purpose (reveal, feedback, wayfinding, coordinate readout); no animation exists without one |

## Anti-Slop / Anti-Template

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| S01 | No generic hero | Screenshot inspection | Confirmed absent | PASS | artifacts/visual/desktop-initial.png — no stock/generated photography, no headline-over-image pattern |
| S02 | No card grids | Screenshot inspection | Confirmed absent | PASS | portfolio is a vertical sequence of full-width plots, not a card grid; `grep -c "border-radius" index.html` = 0, so no rounded-card styling exists to begin with |
| S03 | No generic gradients/pills/glassmorphism | Screenshot inspection | Confirmed absent | PASS with a caveat | `grep` confirms zero `linear-gradient`/`radial-gradient`/`border-radius` anywhere. One `backdrop-filter:blur` exists — on the fixed spine nav bar only, for legibility over scrolling content, not a decorative "glass card." Noted rather than hidden. |
| S04 | No generic fade-up-everything | Code + screenshot inspection | Draw-on is the reveal system, not opacity/translateY | PASS | `grep` confirms no `.fade`/`translateY`-based reveal class exists anywhere in index.html; the only reveal mechanism is `stroke-dashoffset` draw-on |
| S05 | No meaningless cursor effects | Manual review | Cursor crosshair only appears where it carries information (coordinates) | PASS | Fixed the dead-code gap: `.crosshair` is now applied to each `.plot__stage`, paired with a real live coordinate readout (`X ### · Y ###`, mapped to the SVG's own 0–300 viewBox units) that updates on `pointermove` and hides on `pointerleave`. Verified: readout text confirmed as `"X 150 · Y 150"` when the pointer was placed at the exact center of a 300×300-viewBox stage — mathematically correct, not just visually plausible. Screenshot: `artifacts/visual/interaction-coord-readout.png`. |
| T01 | Anti-template test | Thought experiment + structural review | Architecture could NOT be reused for plumber/realtor/landscaper/dentist/laundromat/detailer without a redesign, because the nav/portfolio mechanism (line-scrub) is structurally tied to Clearline's own concept | PASS | The spine is a literal scroll-position scrubber styled as a drafting ruler — repurposing it for a plumber would require either keeping an unmotivated "ruler" nav (a mismatch) or replacing the entire nav/portfolio mechanism, which is exactly the bar this test sets |

## Portfolio & Client Trust

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| P01 | Portfolio is a discovery mechanism | Playwright interaction | Browsing itself demonstrates craft (scrub, not a static grid) | PASS | artifacts/visual/index.md — spine drag test moved through sections including plots in real time |
| P02 | Six distinct project resolutions | Screenshot inspection | Each plot is visually distinct in medium/shape, not a reskin | PASS | artifacts/visual/desktop-initial.png — angular mark, wireframe rect, axonometric cube, editorial rule lines, interface outline, and diagonal speed-lines are six genuinely different SVG compositions, not one shape recolored |
| C01 | What Clearline does is clear | Content read-through | Understandable within the first two viewports | PASS | **Update (identity correction)**: the original thesis ("turns the precision of a drafting line into working identity, interface, and space") was accurate but abstract — it never named a concrete deliverable. Sharpened to: "Websites and digital products, designed and built the way this line was drawn — deliberately, one decision at a time." Now states the literal deliverable (websites, digital products) in the first sentence a visitor reads. |
| C05 | Who is behind Clearline is clear | Content + screenshot read-through | A visitor can name the founder without hunting for an About page | PASS | **New requirement, found genuinely unmet on inspection**: nothing on the entire page named a person before this revision. Fixed with the hero byline ("Evan Greene — independent design & build"), the Process section's reframed intro ("How Evan works through every project..."), and a closing signature credit ("Evan Greene, Clearline Studio") at the end of the Process section — three touches, none a generic founder-bio card, none larger than the Clearline wordmark itself. Verified directly: `grep -c "Evan" index.html` returns 4 — the meta description, the hero byline, the Process intro, and the closing signature — each doing different work (SEO/sharing, identity credit, process voice, signature), not repeats of the same sentence. |
| C02 | What Clearline makes is clear | Content read-through | Portfolio section communicates range | PASS | Six plots span identity, web, spatial, editorial, product, and motion — explicitly labeled per plot |
| C03 | Why the work is good is clear | Content read-through | Process/trust section explains method, not just adjectives | PASS | Process section's five stages each state a concrete exit condition (e.g., "Run it. Screenshot it. Attack it.") rather than generic claims like "years of experience" — deliberately avoids fabricating unverifiable credentials since Clearline is a concept studio (see CONCEPT.md honesty note) |
| C04 | How to contact Clearline is clear | Content read-through | Contact path is unambiguous from any scroll position | PASS | Fixed: added a persistent "Start a line" link docked at the right edge of the spine, visible from every scroll position, using the same spring-scroll as the rest of the nav rather than an inconsistent instant jump. Verified: clicking it from page-load position scrolled to and settled on `#contact`. **Update (real contact info integration)**: the contact section's direct-contact block now shows the real, tiered email (primary) / phone (secondary) / address (tertiary) — `clear.line.studio.creative@gmail.com`, `806-731-9998` (`tel:+18067319998`), `3312 Ault Dr, Amarillo, TX 79121` — replacing the placeholder `studio@clearline.example`. **Update (final refinement)**: the full address (city/state/zip added in this follow-up) is now itself a link to a Google Maps search, styled with a hover-revealed underline so it stays tertiary at rest; verified `href`/`target`/`rel`/`aria-label` directly, and confirmed it wraps within its mobile column with no overflow. |

## Mobile

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| M01 | Mobile composition | Real mobile-viewport screenshot | Not a collapsed desktop layout — recomposed | PASS | artifacts/visual/mobile-initial.png — plot grid stacks to single column, process stages regroup, contact grid stacks; this is genuine recomposition, not a naive collapse (columns/gaps are re-specified per breakpoint, not just narrowed) |
| M02 | Mobile nav | Real mobile-viewport interaction | Line-scrub adapted for touch, operable | PASS | Same CDP touch-event evidence as F09 — real touch gesture, not mouse simulation |
| M03 | Mobile typography | Real mobile-viewport screenshot | Legible, appropriately scaled, no overflow | PASS | artifacts/visual/mobile-initial.png + verify.sh "Overflow issues: none" (after the `.contact-direct` fix) |
| M04 | Mobile portfolio browsing | Real mobile-viewport interaction | Plots reachable and openable by touch | PASS | Reachable via the confirmed touch-drag (F09/M02) and via standard touch-scroll; the Pass 3 alternating-layout fix additionally confirmed via `getBoundingClientRect()` that reversed plots show diagram-before-text on mobile, not just visually assumed |

## Performance

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| PF01 | No large images | Asset audit | Zero raster images over reasonable size (target: no raster images at all — SVG/CSS/canvas only) | PASS | `grep -c "<img" index.html` = 0 (same evidence as F12) |
| PF02 | JS cost | Code review | No animation loop runs when off-screen/idle; listeners are passive where applicable | PASS | `grep` confirms no `setInterval` anywhere; every `requestAnimationFrame` call is triggered by a real event (scroll, drag-release, resize) and terminates, not a perpetual loop; the scroll listener is registered `{passive:true}` |
| PF03 | Layout shift | Playwright + visual diff across load states | No visible reflow after fonts/late content settle | PASS | `.home-thesis`'s `getBoundingClientRect()` compared at `domcontentloaded` vs. +1.5s settle (past the 900ms tick-rebuild and past when Google Fonts would resolve if unblocked): byte-for-byte identical rect both times — no reflow |
| PF04 | Animation cost | Manual review + DevTools-equivalent reasoning | requestAnimationFrame used correctly, no layout thrashing (reading + writing geometry interleaved) | PASS | **Found a real read-after-write layout-thrashing bug during this review**: `render()` was writing SVG attributes (`handle.setAttribute`, `fillLine.setAttribute`) before reading `s.offsetTop` and calling `getBoundingClientRect()` later in the same function — forcing synchronous layout on every scroll frame. Fixed by caching `offsetTop`/`offsetHeight` once in `buildTicks()` (into `ticks[].offsetTop` and a new `darkOffsets` array) and reordering `render()` to do all reads before all writes. Re-verified: `verify.sh` still reports zero console errors and the `onDark` dark-section toggle still fires correctly (tested at both the process section and home section) after the refactor. |

## Delivery Verification (added: real email delivery task)

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| D01 | Form no longer uses mailto for submission | Code audit + DOM check | Zero `mailto:` on the submit control; exactly one remains, on the standalone email link (explicitly permitted) | PASS | `grep -n "mailto:" index.html` returns exactly one match, on `.contact-direct`'s standalone email `<a>`; `#cf-submit` is now a `<button type="submit">` with no `href` at all |
| D02 | Real network delivery attempt occurs | Playwright request listener + temporary non-placeholder key (reverted after the one test, never committed) | A live POST to the delivery endpoint fires on valid submission | PASS | Confirmed a real request to `https://api.web3forms.com/submit` firing, distinct from the "not configured" guard path which correctly fires zero requests |
| D04 | Real access key configured | Code audit | `WEB3FORMS_ACCESS_KEY` holds a real key, not the placeholder | PASS | User supplied the real key (obtained via web3forms.com for `clear.line.studio.creative@gmail.com`) on 2026-09-08; `index.html` line ~588 now holds `c93a9106-98f4-4d02-b5fb-5e541e8ad9bf`, confirmed by direct `grep`. The guard comparison string (line ~615) was deliberately left as the original placeholder text, not updated to match — this is what makes the guard correctly evaluate false now and let the real fetch path run, rather than two matching strings that would trivially and silently re-trigger the "not configured" branch. |
| D03 | Actual delivery to clear.line.studio.creative@gmail.com | Real end-to-end submission with the real key, from this sandbox | Web3Forms accepts the POST and the message arrives in the inbox | **BLOCKED (root cause now conclusively identified — a sandbox egress policy, not the code or the key)** | With the real key configured, submitted a full valid test (desktop and mobile) and got the exact same `net::ERR_TUNNEL_CONNECTION_FAILED` as the earlier placeholder-key test. This time the root cause was confirmed directly rather than inferred: `curl -sS "$HTTPS_PROXY/__agentproxy/status"` shows this session's own outbound proxy log recording `"connect_rejected"`, `"gateway answered 403 to CONNECT (policy denial or upstream failure)"`, `"host": "api.web3forms.com:443"` — the sandbox's own egress gateway refuses the connection before it ever reaches Web3Forms' servers, for every domain not on a small allowlist (the same policy that has blocked Google Fonts, 21st.dev, and all seven benchmark URls this project referenced earlier). No amount of further code changes can fix this from inside this sandbox. **What this does NOT block**: the *deployed* artifact runs in a real visitor's own browser, on their own network, entirely outside this sandbox's proxy — that request path has never been tested and is not subject to this restriction. The correct next verification step is for a human to open the published artifact URL in an ordinary browser and submit a real test there, then check the Gmail inbox — not something achievable from this development environment. |

## FINAL RELEASE GATE

Complete only when every row above is PASS or an explicitly justified, documented BLOCKED (e.g., live external benchmark browsing, which is BLOCKED by the sandbox itself, not by unfinished work; D03 above, for the reasons given). Any FAIL anywhere means **not done**. D03 is BLOCKED, not FAIL — the code path is correct and tested as far as this environment allows; what remains is an external credential only the site owner can obtain.
