# PROGRESS.md — Taqueria Jalisco (complete rebuild)

Read this first if context resets, then QUALITY_GATE.md, then CONCEPT.md and REFERENCE_EXTRACTION.md, then `git status`, then run the site, then continue from the first unresolved item.

## What this replaces

The prior build (invented SVG mural, Bungee font, Tripadvisor rating, paper-tear interaction) is retired per this task's explicit instruction — "OLD WEBSITE: DO NOT USE AS DESIGN FOUNDATION." This is a from-scratch rebuild: new architecture, new visual system (built from the attached PDF's real photographs, not generic Mexican-restaurant convention), new interaction system, new motion language, new typography (Rye + Archivo, matching the real signage rather than a generic poster font).

## State log

- [DONE] **PDF actually inspected**, not skipped. `pdftoppm`/poppler-utils were unavailable and couldn't be installed (network-restricted apt). Installed `pymupdf` via pip instead, rendered all 3 real pages to images, and extracted all 10 embedded photographs directly (cleaner than screenshotting/cropping the composite pages). Full visual extraction written to CONCEPT.md from direct inspection — real storefront (a flat stucco wall painted into a trompe-l'oeil arch/columns), real signage (dark-brown hand-painted capitals, not a script/cursive "taco truck" font), four real murals (serenade, portrait+papel picado, Texas ranch/longhorns, second sign), real food/drink photography.
- [DONE] **Images converted and organized as real site assets**, not left in the PDF or base64-inlined into a bloated HTML file. `assets/*.jpg`, optimized (85% quality JPEG), 280KB total for all 10 images — real, fast, no stock photography.
- [DONE] **Reference sites: genuinely attempted, genuinely blocked, honestly documented.** `WebFetch` tried against 3 of the 9 reference URLs — all returned `EGRESS_BLOCKED`. Rather than fabricate "I visited MakeMePulse and saw X," `REFERENCE_EXTRACTION.md` states this plainly and works from the brief's own stated design principles instead, translated into Taqueria-specific, verifiable equivalents (the arch-frame interaction, the menu accordion) — not lifted from any site this sandbox couldn't reach.
- [DONE] **Full rebuild**: new hero (real storefront photo, real Rye/Archivo typography), a new signature interaction (the "threshold" — a scroll-linked arch-shaped reveal, described below), a real image-choreography murals sequence, a food section built around real photography, a real ~30-item menu with an expand-in-place accordion, the three real Google reviews integrated editorially with real food/atmosphere photos (not generic testimonial cards), and a real visit/contact section.
- [DONE] **Pass 1 verification — structure.** `scripts/verify.js` (the same proven runner from the prior build) reported zero overflow, zero console errors on the first run. That result was not trusted at face value: the actual screenshots were inspected next, per this task's explicit "actually inspect the screenshots" instruction — and that inspection found real defects the automated check missed entirely:
  1. **The signature interaction didn't work at all.** The arch clip-path used `clipPathUnits="userSpaceOnUse"` (real pixel coordinates) but the path itself was written in 0–100 fake-percentage units — meaning the "arch" only ever covered a ~40×45px sliver in the corner of a 1440×900 image, invisible at any real scroll position. Confirmed via live (non-fullPage) screenshots at three real scroll positions inside the sticky stage, not the misleading fullPage capture (which — as established during the prior build — can't represent `position:sticky`/`fixed` elements meaningfully at all). Fixed by rewriting the interaction to compute the arch's actual `d` path in real viewport pixels on every scroll frame, verified afterward with the same three-position check: the arch now genuinely grows from a small centered doorway to full-viewport coverage, confirmed both numerically (the path's left/right x-coordinates) and visually (the mural image is fully visible and correctly framed at the "end" position).
  2. **A hero-photo/HTML-text collision on mobile that looked like an overflow bug but wasn't one.** The photographed sign text ("...ALISCO...RESTAU...", cropped by `object-fit:cover` on a narrow viewport) sat directly above the HTML wordmark, reading as two competing, partially-illegible "JALISCO"s stacked. Direct measurement (`Range.getBoundingClientRect()` on the actual text node) confirmed the HTML wordmark itself was NOT overflowing (225px of text in a 390px box) — the visual problem was the photo's own cropped content, not a CSS bug. Fixed by shifting `object-position` and strengthening the scrim on narrow viewports so the photo reads as atmosphere rather than a second, competing text block — an honest trade-off given a wide landscape photo will always crop significantly on a narrow portrait viewport, not a fully "solved" problem.
- [DONE] **Pass 2 verification — art direction / responsive.** Re-checked all sections at 390/768/1440px after the fixes above. Menu accordion tested for real: clicking a second category closes the first (single-open behavior) and opens the new one, confirmed via `aria-expanded` state changes, not just visual assumption. Mobile menu/hours rows use the same `flex-direction:column` stacking safeguard the prior build had to retrofit after a real bug — built in from the start this time, and confirmed clean with no wrapping issues on first inspection.

## What's genuinely NOT done (named honestly, not hidden)

- **Not four independently-evidenced "passes."** This is Pass 1 (structure) and a real Pass 2 (art direction/responsive) with real bugs found and fixed at each. Passes 3 (deeper interaction/motion polish — e.g. the topbar's onlight/light contrast logic is intersection-based, not luminance-aware, which is a minor known simplification) and 4 (micro-spacing/crop/rhythm polish) are the honest next round, not fabricated here with reused or contrived screenshots.
- **Third-party imagery (Yelp/Google) was not fetched or used** — both domains are blocked by this sandbox's network policy, and the brief itself flags customer-uploaded photos as not automatically licensed for production use. All imagery is the ten real photographs from the user-supplied PDF.
- **Menu categories with zero sourced data (Desayuno, Nachos, Postres, Margaritas, Mixed Drinks, Shots, Kids, To-Go) were not built out** — no placeholder items were invented for them; the menu section explicitly directs visitors to call for those categories instead.

## Pass 3 (this task): brand board + accessibility/asset audit

- [DONE] **Checked whether the new PDF actually had new content before re-processing it.** `md5sum` confirmed it's byte-identical to the prior build's PDF — no new visual material, so no re-extraction work was needed or done. Stated plainly rather than silently re-running the same extraction and padding out "new" evidence from old material.
- [DONE] **Brand board genuinely used, with judgment, not blind compliance.** Two real improvements applied: (1) color variables tightened to the board's exact hex values (three of which had already been independently matched from the raw photos, a real cross-confirmation); (2) the arch motif extended from one interaction into a recurring divider system, matching the board's repeating-arch border. A real implementation bug was found in the divider (a `viewBox`/`preserveAspectRatio="slice"` aspect-ratio mismatch cropped the arch tops off entirely, leaving only vertical lines) — caught via a close-up screenshot, fixed by removing the mismatched viewBox and letting the SVG pattern tile at true 1:1 pixel scale, re-verified visually at desktop and mobile.
- [DONE] **Explicitly declined part of the board**, not silently: its suggested icon row (agave/taco/margarita/pin/fork-knife) conflicts directly with this same task's own anti-AI-slop rule (Section 33 bans exactly this). Also declined its "Reserve a Table" button mockup, since reservations were never confirmed as a real practice. Both documented in CONCEPT.md's Brand Board Addendum with the reasoning, not just omitted.
- [DONE] **Fixed the menu-completeness gap this task explicitly corrected.** The prior build omitted 8 real menu categories (Desayuno, Nachos, Postres, Margaritas, Mixed Drinks/Shots, Hot Drinks, Kids, To-Go) because pricing wasn't confirmed — this task's Section 15 explicitly says that's wrong ("Missing category because of uncertain pricing: FAIL"). Added them back as a real "Also on the menu" accordion category, each item shown with "Ask your server" rather than an invented price or a silent omission.
- [DONE] **Real asset-resolution audit**, not a code-reference assumption. Playwright check of every live `<img>` element (`complete && naturalWidth > 0`) plus a network-failure listener: 12/12 resolved, 0 broken, only the pre-existing Google Fonts block (unrelated to images) in the failure log. Documented in the new `ASSET_MANIFEST.md`, including the one extracted-but-unused asset (`storefront-side.jpg`) — disclosed, not hidden.
- [DONE] **Real accessibility checks, not assumed.** Keyboard `Enter` on the menu accordion verified to produce the same state change as a mouse click. Focus-outline suppression checked directly (`outline-style: auto` confirmed — never overridden). A real, borderline WCAG contrast failure was found by actually computing the relative-luminance contrast ratio (not eyeballing colors): the new "Ask your server" price text measured 4.41:1 against its background, just under the 4.5:1 AA minimum for normal text. Fixed by lightening the color, reverified at 5.60:1.
- [DONE] **Two new control docs created** (`ASSET_MANIFEST.md`, `EXPERIENCE_ARCHITECTURE.md`); two requested docs deliberately folded into existing files rather than duplicated (`VISUAL_SOURCE_ANALYSIS.md` → `CONCEPT.md`'s existing sections; `CONTENT_AUDIT.md` → `QUALITY_GATE.md`'s Q12-15/Q31 rows) — a stated scope decision, not a silent skip. See `QUALITY_GATE.md`'s note at the top of its table.

## Known environment constraints (do not re-discover)

- `pdftoppm`/poppler-utils is not installed and cannot be installed via apt in this sandbox (network-restricted). `pip install pymupdf` works and was used instead — `import fitz` (deprecated alias) or `import pymupdf`, `page.get_pixmap()` for page rasters, `page.get_images()` + `doc.extract_image()` for embedded photos directly (better than screenshotting a composite page and cropping).
- All 9 of this task's reference-site URLs are blocked by this sandbox's network egress policy, same as Yelp/kwickmenu.com/Google Fonts CDN issues documented in earlier builds in this repo.
- `fullPage:true` Playwright screenshots cannot be trusted for `position:fixed` OR `position:sticky` elements — verify those with real (non-fullPage) screenshots at specific, real scroll positions plus direct DOM measurement (`getBoundingClientRect`, attribute values), as established in the prior Taqueria Jalisco build and reconfirmed here for the sticky threshold section.
