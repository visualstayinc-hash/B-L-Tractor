# QUALITY_GATE.md — Taqueria Jalisco (complete rebuild)

Status values: PASS / FAIL / BLOCKED / PARTIAL. PARTIAL is used honestly where real work exists but the brief's maximal ask (e.g. four independently-evidenced visual passes, exhaustive pacing tuning) exceeds what this build cycle actually completed — see PROGRESS.md.

| ID | Requirement | Test method | Pass condition | Status | Evidence |
|---|---|---|---|---|---|
| Q01 | Business identity | Cross-check against brief's Google Business Profile data | Real name/address/phone/rating used, no invented facts | PASS | 2211 Avenue Q, Lubbock TX 79411; (806) 763-7605; 4.5★ Google — matches this task's brief exactly, used consistently in hero, reviews, visit sections and the `tel:`/maps links |
| Q02 | Storefront branding | Direct comparison of the built site against the attached PDF's photos | Real facade photo used; real signage lettering character translated into site typography | PASS | `assets/storefront-hero.jpg` is the actual facade photo, used as the hero background. Typography switched from the prior build's Bungee (rounded, bubbly) to Rye (Google Fonts' actual wanted-poster/Western-signage face) specifically because Bungee doesn't match the real dark-brown blocky capitals photographed on the building — see CONCEPT.md's Typographic Character section |
| Q03 | Brown/rustic atmosphere | Color audit against the real photos | Palette sourced from the actual building, not invented "Mexican restaurant" colors | PASS | `--ochre`/`--ink`/`--red`/`--teal` CSS variables sourced directly from the facade photo's actual paint colors (ochre stucco, dark-brown ink, brick-red columns, muted teal agave) — documented with hex ranges in CONCEPT.md's Color System section |
| Q04 | Mural translation | Compare `#murals` section against the PDF | Real murals used as real photography, not generic SVG/clip-art | PASS | All 4 real murals (serenade, portrait+papel picado, ranch/longhorns, second sign) used as actual photographs in `#murals`, each with distinct framing (alternating left/right, one full-width). The prior build's invented SVG sun/agave illustration is fully retired — `grep` confirms zero `<svg>` decorative-illustration content remains (the only SVGs in the file are the arch-clip mechanism and the favicon) |
| Q05 | Image selection | Audit which of the 10 real images were used and how | Curated by role, not "every image dumped in" | PASS | All 10 images used, each assigned one role per CONCEPT.md's Image Library table (hero/storefront/mural ×4/food ×2/drink ×2) — none reused in a way that reads as filler |
| Q06 | Image composition | Screenshot inspection at desktop/tablet/mobile | Images function as composition (scale changes, alternating layout, full-bleed moments), not equal-size cards | PARTIAL | Real variation exists: full-bleed hero, the scroll-driven arch reveal, alternating mural rows (`.mural-row--rev`), a full-bleed food hero with a 2-up detail row beneath. Honest gap: this doesn't reach the brief's most aggressive ask (images overlapping type, breaking the grid, aggressive asymmetric cropping) — a real Pass 4 refinement, not attempted here |
| Q07 | Food story | Screenshot inspection of `#food` | Hero food moment + secondary/detail food, not all dishes equal | PASS | `food-combo-plate.jpg` full-bleed as the hero moment; `food-crispy-tacos.jpg` + `drink-margarita-dosxx.jpg` as a secondary detail pair beneath |
| Q08 | Menu completeness | Cross-check every displayed item/price against the brief's researched list | Substantial real menu, no placeholder items, no invented prices | PASS | 31 real items across 6 categories (Antojitos, Ensaladas, Especialidades de la Casa, A La Carte, Combinaciones, plus 4 daily specials + Pozole) — every single item/price traces directly to the brief's own researched list. Approximate prices (marked in the brief itself as "approximately") are visually flagged `approx.` in the UI, not presented as exact. Categories with zero sourced data (Desayuno, Nachos, Postres, Margaritas, Mixed Drinks, Shots, Kids, To-Go) were deliberately NOT built out with invented items — the menu section explicitly says so and points to the phone number instead |
| Q09 | Menu interaction | Playwright click test | Real state change (expand/collapse), not decorative | PASS | Verified via Playwright: clicking a category sets `aria-expanded="true"` on it and `"false"` on any previously-open category — confirmed as an actual state change across a real click, not assumed from markup. Default: first category (Antojitos) open on load |
| Q10 | Google 4.5★ | Grep for rating citations | 4.5★ used consistently; Yelp/Tripadvisor NOT substituted | PASS | `grep -c "4.5"` returns 4 (hero meta, reviews eyebrow line, review-source line, and visit section) — no Tripadvisor 4.3★ or Yelp citation remains anywhere in the file (that was the prior build's source; this task's brief explicitly named Google as authoritative) |
| Q11 | Google testimonial 01 | Text diff against the brief | Candace Brown's review used verbatim | PASS | Full text reproduced exactly as supplied, including the original capitalization/punctuation quirks — not smoothed into marketing copy |
| Q12 | Google testimonial 02 | Text diff against the brief | Second review (unnamed) used verbatim | PASS | Full text reproduced exactly as supplied, presented as "Google review" since no name was given — not fabricated |
| Q13 | Google testimonial 03 | Text diff against the brief | Third review (unnamed) used verbatim | PASS | Same treatment as Q12 |
| Q14 | Typography | Visual comparison against the real signage photos | Typography reflects the actual painted lettering, not a generic decorative font | PASS | Rye (display) + Archivo (body) — see Q02. Verified rendering correctly via screenshot (not just declared in CSS) at all tested viewports |
| Q15 | Visual hierarchy | Screenshot inspection per section | Consistent eyebrow → h2 → lede pattern, legible at every breakpoint | PASS | Confirmed consistent across hero/murals/food/menu/reviews/visit in desktop, tablet, and mobile screenshots |
| Q16 | Narrative pacing | Full-scroll screenshot review | Genuine loud/quiet contrast, not uniform section rhythm | PARTIAL | Real contrast exists (full-bleed hero → tall scroll-driven threshold → quiet cream murals → loud full-bleed food → dense dark menu → quiet cream reviews → warm ochre visit) — a real, felt rhythm, but not exhaustively timed/tuned against each other; a genuine Pass 4 opportunity |
| Q17 | Signature interaction | Playwright scroll-position + attribute checks | Interaction emerges from the real building, functions as a real scroll-linked reveal | PASS | The arch-threshold interaction is modeled on the real trompe-l'oeil facade arch. A real implementation bug (percentage-unit path coordinates paired with pixel-unit clipPath, causing the arch to only ever render a ~40px sliver) was found via live scroll-position screenshots and fixed by computing the path in real viewport pixels every scroll frame — re-verified at 3 real scroll positions, confirmed both numerically (path coordinates literally growing from a small centered arch to full-viewport coverage) and visually |
| Q18 | Motion | Code + behavior audit against Trigger/Purpose/Direction/Timing/Easing/Hierarchy | Every meaningful animation documented and purposeful | PASS | Threshold arch: Trigger=scroll position within `#threshold`; Purpose=reveal the mural section as a "walk through the doorway"; Direction=aperture grows outward from center; Timing=continuous, tied to scroll (not a fixed duration); Easing=none (1:1 scroll-linked, deliberately not eased, so it never lags real scroll input); Hierarchy=primary/only major scroll-driven interaction, everything else is static or discrete (menu accordion). `prefers-reduced-motion` forces the arch to its fully-open end state immediately |
| Q19 | Navigation | Screenshot + click test | Navigation reflects the site's own architecture, not a generic navbar | PASS | A minimal fixed topbar (restaurant name + a real call button) — no hamburger menu, no nav links list; wayfinding is scroll-based, appropriate for a single narrative page |
| Q20 | Mobile | Screenshot inspection at 390px, contrasted with desktop | Recomposed, not desktop-stacked | PASS | Real bug found and fixed: the hero photo's `object-fit:cover` crop collided visually with the HTML wordmark on narrow viewports (two partially-legible "JALISCO"s stacked) — confirmed via direct text-measurement (`Range.getBoundingClientRect`) that the HTML text itself was NOT overflowing (225px in a 390px box), isolating the real cause to photo cropping, not a CSS bug. Fixed with mobile-specific `object-position` and a strengthened scrim. Menu/hours rows use column-stacking below 420px, built in from the start (a bug the prior build had to retrofit) |
| Q21 | Anti-template | Self-critique: could this become a different restaurant by swapping logo/colors/copy/photos? | NO required | PASS | Unlike the prior build's invented SVG illustration, every image, the specific arch shape (modeled on this building's actual trompe-l'oeil facade), the specific menu items/prices, and the three specific reviews are this restaurant's real material — swapping them out isn't a palette change, it's building a different site from different source photographs and different real reviews |
| Q22 | Anti-AI-slop | Grep audit | No generic hero/card-grid/pill/gradient-blob/glassmorphism/clip-art patterns | PASS | `grep` confirms: 2 `border-radius` uses (1–2px, not pill-shaped), 3 `gradient` uses (all functional photo-legibility scrims, none decorative), 0 backdrop-filter/glassmorphism, 0 stock `<img>` (all 10 images are the real supplied photographs), 0 generic icon usage beyond the menu accordion's own +/− indicator |
| Q23 | Restaurant specificity | Content review | Content couldn't apply to a generic Mexican restaurant | PASS | The ranch/longhorn mural section explicitly calls out "no generic Mexican restaurant template would ever paint this wall" — a real, specific, unexpected fact this build leans into rather than smooths over |
| Q24 | Client trust | Content + link audit | Real facts throughout, no fabricated results/testimonials/counts | PASS | Real reviews verbatim (Q11-13), real rating (Q10), real hours/address/phone with working `tel:`/maps links, and an explicit, visible disclosure of which menu prices are approximate rather than confirmed |
| Q25 | Performance | Asset size audit | No unnecessary weight | PASS | All 10 images optimized to JPEG (85% quality): 280KB total. No raster bloat, no unused libraries, single external font request (Google Fonts, same as every other site in this repo) |

## Passes actually completed

**Pass 1 — Structure.** Built, ran, captured, and — critically — actually inspected the screenshots rather than trusting the automated overflow/console check alone. Found and fixed the signature interaction's core bug (see Q17) purely through that inspection; the automated check reported zero issues the whole time.

**Pass 2 — Art direction / responsive.** Re-verified all sections at 390/768/1440px after the Pass 1 fixes. Found and fixed the mobile hero photo/text collision (Q20) through the same discipline — inspecting the actual rendered screenshot, not assuming the CSS was correct because no automated check flagged it.

Passes 3 (deeper motion/interaction polish) and 4 (micro-spacing, crop rhythm, pacing tuning) are the honest next round — named specifically in Q06/Q16, not silently skipped or falsely claimed complete.
